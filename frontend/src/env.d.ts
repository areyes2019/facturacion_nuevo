/**
 * Identificador de la compilación, inyectado por Vite (ver `vite.config.ts` y
 * 044-sesion-caida-y-pantallas-trabadas.md). Sirve para distinguir "la recarga trajo una versión
 * nueva" de "la recarga devolvió exactamente lo mismo".
 */
declare const __BUILD_ID__: string
