{
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.vitest.tsbuildinfo",
    // Los tests leen el fixture compartido del repositorio con node:fs, así que necesitan los
    // tipos de Node además de los del navegador (ver 011-precio-proveedor-utilidad.md).
    "types": ["vite/client", "node"]
  },
  "include": ["src/**/*.test.ts"],
  "exclude": []
}
